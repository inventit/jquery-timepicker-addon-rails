RSpec.describe Jquery::Timepicker::Addon::Rails do
  it "has a version number" do
    expect(Jquery::Timepicker::Addon::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
